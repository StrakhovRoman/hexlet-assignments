h1= @task.name
p= @task.description
p= @task.status
p= @task.performer
p= @task.completed
p
  |  Creator: 
  i= @task.creator
small i= @task.created_at.in_time_zone('Moscow').strftime('%H:%m %d.%m.%Y')
hr
p= link_to 'Edit', edit_task_path(@task)
p= button_to 'Delete', task_path(@task), method: :delete, data: { confirm: 'Are you sure?' }
p= link_to 'Back', root_path
