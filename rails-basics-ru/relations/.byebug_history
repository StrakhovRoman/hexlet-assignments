Status.random_status.id
random_status = [:first, :second, :three].sample
random_status = [:first :second :three].sample
Status.random_status.id
random_status = %w[first second three].sample
random_status = %w[first, second, three].sample

Status.[first, second, three].sample.id
Status.[first, second, three].id
Status.['New' 'Progress' 'Done'].id
Status.['New','Progress','Done'].id
Status.sample.id
Status.first.class
Status.first
Status
Status.id
clear
Status.method
Status.methods
Status(:one)
Status.first.id
Status.second
Status.first
Status.class
Status.count
Status.all
