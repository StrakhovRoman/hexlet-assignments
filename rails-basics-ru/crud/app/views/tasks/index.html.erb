<h1>Tasks</h1>

<%= link_to 'New task', new_task_path %>
<hr>

<% @tasks.each do |task| %>
  <p><%= link_to task.name, task_path(task.id) %></p>
  <small><i><%= task.name %> <%= task.created_at.strftime(
    '%H:%m %d.%m.%Y') %> </i></small>
  <hr>
<%end%>
